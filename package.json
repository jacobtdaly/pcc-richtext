{
  "name": "pcc-richtext",
  "private": true,
  "version": "0.1.3",
  "devDependencies": {
    "esbuild": "^0.25.0"
  },
  "scripts": {
    "build": "esbuild src/index.js --bundle --format=iife --define:__VERSION__='\"'$npm_package_version'\"' --outfile=dist/pcc-richtext.js"
  }
}
